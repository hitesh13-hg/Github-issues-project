// import { fromJS } from 'immutable';
// import { selectCapDomain } from '../selectors';

describe('selectCapDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
